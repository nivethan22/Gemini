name: "⚡ EnigMano Win11 + 128 Cores Deployment (Optimized)"

on:
  workflow_dispatch:
    inputs:
      INSTANCE:
        description: "Instance number to deploy (e.g., 1, 2, 3...)"
        required: true
        default: "1"
      CRD_PIN:
        description: "PIN for the Remote Desktop (6 digits)"
        required: true
        default: "123456"
      CRD_TOKEN:
        description: "Paste the Auth Code (starts with 4/...) or full command"
        required: true
        default: ""
      TAILSCALE_AUTHKEY:
        description: "Tailscale Auth Key (starts with tskey-...). Optional."
        required: false
        default: ""

jobs:
  deploy-enigmano:
    name: "🚀 Deploy EnigMano Instance ${{ github.event.inputs.INSTANCE }}"
    runs-on: windows-latest # Changed from windows-11-arm for compatibility

    env:
      INSTANCE_ID: ${{ github.event.inputs.INSTANCE }}
      CRD_PIN: ${{ github.event.inputs.CRD_PIN }}
      CRD_TOKEN: ${{ github.event.inputs.CRD_TOKEN }}
      TAILSCALE_AUTHKEY: ${{ github.event.inputs.TAILSCALE_AUTHKEY }}
      REPO: ${{ github.repository }}
      WORKFLOW_FILE: "enigmano-Win-11-128-Cores.yml"
      DEPLOYMENT_ID: ${{ github.run_id }}

    steps:
      - name: 📌 Deployment Parameters
        shell: pwsh
        run: |
          Write-Host "==============================================="
          Write-Host "🔹 EnigMano Instance     : $env:INSTANCE_ID"
          Write-Host "==============================================="

      - name: 🔐 Validate Inputs
        shell: pwsh
        run: |
          if (-not $env:CRD_TOKEN) { Write-Error "❌ Missing CRD Token." ; exit 1 }
          if (-not $env:TAILSCALE_AUTHKEY) { Write-Warning "⚠️ Skipping Tailscale." }
          Write-Host "✅ Inputs validated."

      ---

      - name: 💻 Install Virtual Display Driver (Fixes Lag)
        shell: pwsh
        run: |
          Write-Host "🌐 Downloading and Installing ViGEm Bus Driver (Virtual Display)..."
          $url = "https://github.com/ViGEm/ViGEmBus/releases/download/v1.17.333.0/ViGEmBus_Setup_1.17.333.0.exe" # Stable driver for headless sessions
          $output = "$env:TEMP\vigem.exe"
          Invoke-WebRequest -Uri $url -OutFile $output -UseBasicParsing

          Write-Host "💾 Installing ViGEm..."
          Start-Process -FilePath "$output" -ArgumentList "/S" -Wait # /S is silent install

          Write-Host "✅ Virtual Display Driver installed. This prevents display lag/sleep."
          
      ---

      - name: ⚙️ Install & Configure Chrome Remote Desktop
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # Download, Install, and Register CRD (using the robust remoting_start_host.exe method)
          Write-Host "🌐 Downloading & Installing CRD Host..."
          $url = "https://dl.google.com/dl/edgedl/chrome-remote-desktop/chromeremotedesktophost.msi"
          $output = "$env:TEMP\crd.msi"
          Invoke-WebRequest -Uri $url -OutFile $output -UseBasicParsing
          Start-Process msiexec.exe -ArgumentList "/i `"$output`" /qn" -Wait

          Write-Host "🔑 Registering CRD Host..."
          $crdExecutable = "${Env:PROGRAMFILES(X86)}\Google\Chrome Remote Desktop\CurrentVersion\remoting_start_host.exe"
          
          # Logic to handle full command paste vs. just the code
          $authToken = $env:CRD_TOKEN
          if ($authToken -match 'code="([^"]+)"') { $authToken = $matches[1] }

          $hostName = "EnigMano-Instance-$env:INSTANCE_ID"

          & $crdExecutable --code="$authToken" `
                           --redirect-url="https://remotedesktop.google.com/_/oauthredirect" `
                           --name="$hostName" `
                           --pin="$env:CRD_PIN"

          Write-Host "✅ CRD setup complete."
          
      ---

      - name: ☀️ Install & Configure Sunshine (for Moonlight)
        shell: pwsh
        run: |
          Write-Host "🌐 Downloading Sunshine (Streaming Server for Moonlight)..."
          # Get the latest stable release of Sunshine
          $url = "https://github.com/LizardByte/Sunshine/releases/download/v0.22.0/sunshine-windows-portable.zip"
          $zipFile = "$env:TEMP\sunshine.zip"
          $installPath = "C:\Sunshine"

          Invoke-WebRequest -Uri $url -OutFile $zipFile -UseBasicParsing
          
          Write-Host "💾 Extracting Sunshine..."
          Expand-Archive -Path $zipFile -DestinationPath $installPath -Force

          Write-Host "🔥 Starting Sunshine Service..."
          # Start the server (Moonlight client can connect after this)
          Start-Process -FilePath "$installPath\sunshine.exe" -ArgumentList "-d" -Wait -PassThru # -d runs in background
          
          Write-Host "✅ Sunshine (Moonlight Server) is now running."
          Write-Host "   You can access the UI via CRD or Tailscale at https://127.0.0.1:47990/"
          
      ---

      - name: 🦎 Install & Connect Tailscale
        if: ${{ github.event.inputs.TAILSCALE_AUTHKEY != '' }}
        shell: pwsh
        run: |
          Write-Host "🌐 Downloading Tailscale..."
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup.exe"
          $output = "$env:TEMP\tailscale-setup.exe"
          Invoke-WebRequest -Uri $url -OutFile $output -UseBasicParsing
          Start-Process -FilePath "$output" -ArgumentList "/quiet" -Wait

          Write-Host "🔑 Authenticating Tailscale..."
          $tsPath = "C:\Program Files\Tailscale\tailscale.exe"
          
          if (Test-Path $tsPath) {
             & $tsPath up --authkey $env:TAILSCALE_AUTHKEY --hostname "EnigMano-$env:INSTANCE_ID" --unattended
             Write-Host "✅ Tailscale Connected!"
             & $tsPath ip -4
          } else {
             Write-Error "❌ Tailscale installation failed."
          }
          
      ---

      - name: 🏃 Keep Runner Alive
        shell: pwsh
        run: |
          Write-Host "✅ Runner is active. Session is ready."
          Write-Host "👉 Access via CRD: https://remotedesktop.google.com/access"
          Write-Host "👉 Access via Moonlight: Use the Tailscale IP (if provided)."
          
          $seconds = 21600 # 6 hours
          while ($seconds -gt 0) {
            Start-Sleep -Seconds 60
            $seconds -= 60
            if ($seconds % 300 -eq 0) {
              Write-Host "⏳ Time remaining: $([math]::Round($seconds/60)) minutes..."
            }
          }

      - name: 💠 Final Status
        if: always()
        shell: pwsh
        run: Write-Host "🏁 Session Finished."
