Name: "⚡ Win11 ARM Pro Deployment with Virtual Display"

on:
  # Allows manual running of the workflow
  workflow_dispatch:
    inputs:
      INSTANCE:
        description: "Instance identifier (e.g., 1, 2, 3...)"
        required: true
        default: "1"
      CRD_PIN:
        description: "PIN for the Remote Desktop (6 digits)"
        required: true
        default: "123456"
      CRD_TOKEN:
        description: "Paste the CRD Auth Code (starts with 4/...)"
        required: true
        default: ""
      ZEROTIER_NETWORK_ID:
        description: "Your 16-digit ZeroTier Network ID (Required for ZT install)"
        required: true
        default: ""
      
jobs:
  deploy-pro-instance:
    name: "🚀 Deploy Instance ${{ github.event.inputs.INSTANCE }}"
    # Use the Windows 11 ARM runner
    runs-on: windows-11-arm 

    env:
      INSTANCE_ID: ${{ github.event.inputs.INSTANCE }}
      CRD_PIN: ${{ github.event.inputs.CRD_PIN }}
      CRD_TOKEN: ${{ github.event.inputs.CRD_TOKEN }}
      SESSION_DURATION_HOURS: 6 # Maximum duration for GitHub Actions

    steps:
      - name: 📌 Deployment Parameters
        shell: pwsh
        run: |
          Write-Host "==============================================="
          Write-Host "🔹 Instance ID            : $env:INSTANCE_ID"
          Write-Host "🔹 ZeroTier Network ID    : $env:ZEROTIER_NETWORK_ID"
          Write-Host "🔹 Session Duration       : $env:SESSION_DURATION_HOURS hours"
          Write-Host "==============================================="

      - name: 🔐 Validate Inputs
        shell: pwsh
        run: |
          if (-not $env:CRD_TOKEN) { Write-Error "❌ Missing CRD Token." ; exit 1 }
          if (-not $env:ZEROTIER_NETWORK_ID) { Write-Error "❌ Missing ZeroTier Network ID." ; exit 1 }
          Write-Host "✅ Inputs validated."

      - name: ⚙️ Install & Configure Chrome Remote Desktop and Display
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          
          # 1. Download & Install CRD
          Write-Host "🌐 Downloading & Installing CRD Host..."
          $crdUrl = "https://dl.google.com/dl/edgedl/chrome-remote-desktop/chromeremotedesktophost.msi"
          $crdOutput = "$env:TEMP\crd.msi"
          Invoke-WebRequest -Uri $crdUrl -OutFile $crdOutput -UseBasicParsing
          Start-Process msiexec.exe -ArgumentList "/i `"$crdOutput`" /qn" -Wait
          
          # Give the CRD service a moment to start after installation
          Write-Host "⏳ Waiting for CRD service initialization..."
          Start-Sleep -Seconds 5 

          # 2. Register CRD using the binary
          Write-Host "🔑 Registering CRD Host..."
          $crdExecutable = "${Env:PROGRAMFILES(X86)}\Google\Chrome Remote Desktop\CurrentVersion\remoting_start_host.exe"
          
          # Extract the token if the user pasted the full command
          $authToken = $env:CRD_TOKEN
          if ($authToken -match 'code="([^"]+)"') { $authToken = $matches[1] }

          $hostName = "Win11-ARM-Instance-$env:INSTANCE_ID"

          & $crdExecutable --code="$authToken" `
                           --redirect-url="https://remotedesktop.google.com/_/oauthredirect" `
                           --name="$hostName" `
                           --pin="$env:CRD_PIN"
          
      - name: 🏃 Keep Runner Alive (Max 6 Hours)
        shell: pwsh
        run: |
          $durationSeconds = $env:SESSION_DURATION_HOURS * 3600
          Write-Host "✅ Runner is active for $env:SESSION_DURATION_HOURS hours. Session is ready."
          Write-Host "👉 Connect via CRD: https://remotedesktop.google.com/access"
          Write-Host "👉 Connect via ZeroTier/Sunshine: Find the device's assigned ZeroTier IP (requires authorization)."
          
          $seconds = $durationSeconds
          while ($seconds -gt 0) {
            Start-Sleep -Seconds 60
            $seconds -= 60
            if ($seconds % 300 -eq 0) { # Log every 5 minutes
              $minutesRemaining = [math]::Round($seconds/60)
              Write-Host "⏳ Time remaining: $minutesRemaining minutes..."
            }
          }

      - name: 💠 Final Status
        if: always()
        shell: pwsh
        run: |
          Write-Host "✅ Session Finished. Runner will now clean up."
