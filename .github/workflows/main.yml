name: "⚡ EnigMano Win11 + 128 Cores Deployment (FIXED)"

on:
  workflow_dispatch:
    inputs:
      INSTANCE:
        description: "Instance number to deploy (e.g., 1, 2, 3...)"
        required: true
        default: "1"
      CRD_PIN:
        description: "PIN for the Remote Desktop (6 digits)"
        required: true
        default: "123456"
      CRD_TOKEN:
        description: "Paste the CRD Auth Code (starts with 4/...) or full command"
        required: true
        default: ""
      ZEROTIER_NETWORK_ID:
        description: "Your 16-digit ZeroTier Network ID. Optional."
        required: false
        default: ""

jobs:
  deploy-enigmano:
    name: "🚀 Deploy EnigMano Instance ${{ github.event.inputs.INSTANCE }}"
    runs-on: windows-11-arm # CHANGED BACK AS REQUESTED

    env:
      INSTANCE_ID: ${{ github.event.inputs.INSTANCE }}
      CRD_PIN: ${{ github.event.inputs.CRD_PIN }}
      CRD_TOKEN: ${{ github.event.inputs.CRD_TOKEN }}
      ZEROTIER_NETWORK_ID: ${{ github.event.inputs.ZEROTIER_NETWORK_ID }}
      REPO: ${{ github.repository }}
      WORKFLOW_FILE: "enigmano-Win-11-128-Cores.yml"
      DEPLOYMENT_ID: ${{ github.run_id }}

    steps:
      - name: 📌 Deployment Parameters
        shell: pwsh
        run: |
          Write-Host "==============================================="
          Write-Host "🔹 EnigMano Instance      : $env:INSTANCE_ID"
          Write-Host "==============================================="

      - name: 🔐 Validate Inputs
        shell: pwsh
        run: |
          if (-not $env:CRD_TOKEN) { Write-Error "❌ Missing CRD Token." ; exit 1 }
          if (-not $env:ZEROTIER_NETWORK_ID) { Write-Warning "⚠️ Skipping ZeroTier." }
          Write-Host "✅ Inputs validated."

      - name: ⚙️ Install & Configure Chrome Remote Desktop 
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # 1. Download & Install CRD
          Write-Host "🌐 Downloading & Installing CRD Host..."
          $url = "https://dl.google.com/dl/edgedl/chrome-remote-desktop/chromeremotedesktophost.msi"
          $output = "$env:TEMP\crd.msi"
          Invoke-WebRequest -Uri $url -OutFile $output -UseBasicParsing
          Start-Process msiexec.exe -ArgumentList "/i `"$output`" /qn" -Wait

          # 2. Register CRD using the binary
          Write-Host "🔑 Registering CRD Host..."
          $crdExecutable = "${Env:PROGRAMFILES(X86)}\Google\Chrome Remote Desktop\CurrentVersion\remoting_start_host.exe"
          
          $authToken = $env:CRD_TOKEN
          if ($authToken -match 'code="([^"]+)"') { $authToken = $matches[1] }

          $hostName = "EnigMano-Instance-$env:INSTANCE_ID"

          & $crdExecutable --code="$authToken" `
                           --redirect-url="https://remotedesktop.google.com/_/oauthredirect" `
                           --name="$hostName" `
                           --pin="$env:CRD_PIN"

          Write-Host "✅ CRD setup complete."
          
      - name: ☀️ Install & Configure Sunshine (for Moonlight)
        shell: pwsh
        run: |
          Write-Host "🌐 Downloading Sunshine (Streaming Server)..."
          $url = "https://github.com/LizardByte/Sunshine/releases/download/v0.22.0/sunshine-windows-portable.zip"
          $zipFile = "$env:TEMP\sunshine.zip"
          $installPath = "C:\Sunshine"

          Invoke-WebRequest -Uri $url -OutFile $zipFile -UseBasicParsing
          Expand-Archive -Path $zipFile -DestinationPath $installPath -Force

          Write-Host "🔍 Locating Sunshine Executable..."
          # FIX: Recursively find sunshine.exe due to subfolder extraction
          $exePath = Get-ChildItem -Path $installPath -Filter "sunshine.exe" -Recurse | Select-Object -First 1 -ExpandProperty FullName
          
          if (-not $exePath) { 
              Write-Error "❌ Could not find sunshine.exe in extracted files!" 
              exit 1 
          }

          Write-Host "🔥 Starting Sunshine Service from: $exePath"
          Start-Process -FilePath $exePath -WindowStyle Hidden
          
          Write-Host "✅ Sunshine (Moonlight Server) is now running in background."
          
      - name: 🌍 Install & Connect ZeroTier
        if: ${{ github.event.inputs.ZEROTIER_NETWORK_ID != '' }}
        shell: pwsh
        run: |
          Write-Host "🌐 Downloading ZeroTier One..."
          $url = "https://download.zerotier.com/dist/ZeroTier%20One.msi"
          $output = "$env:TEMP\zerotier.msi"
          Invoke-WebRequest -Uri $url -OutFile $output -UseBasicParsing

          Write-Host "💾 Installing ZeroTier..."
          Start-Process msiexec.exe -ArgumentList "/i `"$output`" /qn /norestart" -Wait
          
          # FIX: Wait 5 seconds for the ZeroTier service to initialize and files to be written
          Write-Host "⏳ Waiting for ZeroTier service to initialize..."
          Start-Sleep -Seconds 5 

          Write-Host "🔗 Joining ZeroTier Network..."
          $ztPath = "$env:ProgramFiles\ZeroTier\One\zerotier-cli.exe"
          
          if (Test-Path $ztPath) {
              & $ztPath join $env:ZEROTIER_NETWORK_ID
              Start-Sleep -Seconds 10
              
              Write-Host "✅ ZeroTier Joined. Remember to authorize this node in your Central console."
              & $ztPath info
          } else {
              Write-Error "❌ ZeroTier installation failed: zerotier-cli.exe not found at $ztPath."
              exit 1 
          }
          
      - name: 🏃 Keep Runner Alive
        shell: pwsh
        run: |
          Write-Host "✅ Runner is active. Session is ready."
          Write-Host "👉 Connect via CRD: https://remotedesktop.google.com/access"
          
          $seconds = 21600 # 6 hours
          while ($seconds -gt 0) {
            Start-Sleep -Seconds 60
            $seconds -= 60
            if ($seconds % 300 -eq 0) {
              Write-Host "⏳ Time remaining: $([math]::Round($seconds/60)) minutes..."
            }
          }

      - name: 💠 Final Status
        if: always()
        shell: pwsh
        run: |
          Write-Host "✅ Session Finished."
          Write-Host "🔋 Powered by: SHAHZAIB-YT"
